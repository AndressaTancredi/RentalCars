<h1><%= @client.name %></h1>
<dl>
  <dt>Email:</dt>
  <dd><%= @client.email %></dd>
  <dt>Documento:</dt>
  <dd><%= @client.formatted_document%></dd>
</dl>
<<section>
  <h1> Agendamentos</h1>
  <% @client.rentals.where(status: :scheduled).each do |rental|%>
    <div>
      <%= rental.code %>
      <%= localize%>
    </div>
    <%= link_to  "Iniciar Locação", model %>
  <% end %>  
</section>>